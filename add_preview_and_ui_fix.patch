*** main_ui.py (v1.05 β)
@@ top of file, with your other imports:
-from PyQt6 import QtWidgets
+from PyQt6 import QtWidgets, QtGui, QtCore
+from qr_watermark import watermark_image      # ← new import

@@ class WatermarkWizard(...):
     def __init__(...):
         super().__init__(...)
         self.ui = Ui_MainWindow()   # or however you load the .ui
         self.ui.setupUi(self)
+        # bump version
+        self.setWindowTitle(self.windowTitle() + "  v1.06 β")

         # … your existing signal/slot wiring …
-        self.ui.shadowColorBtn.clicked.connect(self.pick_shadow_color)
+        # move Shadow Color button under Text Color button
+        # (remove it from its old spot and re-add below the TextColor button)
+        parent_layout = self.ui.someGridLayout  # replace with your actual layout object
+        parent_layout.removeWidget(self.ui.shadowColorBtn)
+        row, col, rowSpan, colSpan = parent_layout.getItemPosition(
+            parent_layout.indexOf(self.ui.textColorBtn)
+        )
+        parent_layout.addWidget(self.ui.shadowColorBtn, row + 1, col, 1, colSpan)
+        self.ui.shadowColorBtn.clicked.connect(self.pick_shadow_color)

+        # —––– ADD LIVE PREVIEW CONTROLS –––––—
+        self.previewBtn = QtWidgets.QPushButton("Preview", self)
+        self.previewLabel = QtWidgets.QLabel(self)
+        self.previewLabel.setFixedSize(400, 300)
+        self.previewBtn.clicked.connect(self.show_preview)
+        # stick them at the bottom of your control panel:
+        parent_layout.addWidget(self.previewBtn, row + 2, 0, 1, 2)
+        parent_layout.addWidget(self.previewLabel, row + 3, 0, 1, 2)

@@ class WatermarkWizard(...):
     def save_and_run(self):
         # your existing save logic
         os.system("python qr_watermark.py")

+    def show_preview(self):
+        """
+        Gather current UI config, let user pick one sample image,
+        and display the watermarked result in previewLabel.
+        """
+        # build cfg dict from UI
+        cfg = {
+            **self.config,
+            "input_dir": self.ui.inputDir.text(),
+            "text_overlay": self.ui.overlayText.toPlainText(),
+            "qr_link": self.ui.qrLink.text(),
+            "font_size_ratio": self.ui.fontSizeSlider.value() / 100,
+            "text_padding_bottom_ratio": self.ui.textPaddingSlider.value() / 100,
+            "qr_padding_vh_ratio": self.ui.qrPaddingSlider.value() / 100,
+            "text_color": self.text_color.name(),
+            "shadow_color": self.shadow_color.name()
+        }
+        # ask for a sample image
+        sample, _ = QtWidgets.QFileDialog.getOpenFileName(
+            self, "Select Sample Image", "", "Images (*.png *.jpg)"
+        )
+        if not sample:
+            return
+
+        # generate preview
+        pil_img = watermark_image(sample, cfg)
+
+        # convert to QPixmap
+        raw = pil_img.tobytes("raw", "RGBA")
+        qimg = QtGui.QImage(
+            raw, pil_img.width, pil_img.height,
+            QtGui.QImage.Format.Format_RGBA8888
+        )
+        pix = QtGui.QPixmap.fromImage(qimg).scaled(
+            self.previewLabel.width(),
+            self.previewLabel.height(),
+            QtCore.Qt.AspectRatioMode.KeepAspectRatio
+        )
+        self.previewLabel.setPixmap(pix)
